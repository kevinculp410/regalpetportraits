# Copy this file to `.env` in the repo root and fill values.
# The server and scripts load env via `dotenv` (see src/server.js and scripts/*).

# --- Server ---
NODE_ENV=development
PORT=3002

# --- Database ---
DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/DBNAME
DB_SCHEMA=pet_portraits
IDLE_TIMEOUT_MS=1200000

# --- Mailjet ---
MAILJET_API_KEY=your-mailjet-api-key
MAILJET_SECRET_KEY=your-mailjet-secret-key
MAILJET_FROM_EMAIL=no-reply@emailpetportraits.com
MAILJET_FROM_NAME=Regal Pet Portraits
ADMIN_EMAIL=admin@example.com

# --- AWS/S3 (uploads/results) ---
AWS_REGION=us-east-1
S3_BUCKET=your-s3-bucket-name
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key

# --- Stripe ---
# NOTE: Do NOT commit your real live keys. Use dashboard or local .env.
STRIPE_SECRET_KEY=sk_live_your_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here
BASE_PRICE_ID=price_live_base
UPSCALE_PRICE_ID=price_live_upscale

# --- Optional front-end API base override (normally handled by public/env.js) ---
# API_BASE_URL=http://localhost:3002